# Optional comma-separated exact origins for trusted cross-origin browser clients.
# Same-origin browser requests and server-to-server MCP clients need no entry.
ALLOWED_ORIGINS=

# The hosting provider supplies PORT in production.
PORT=8787
